{
  "name": "markdown-renderer",
  "version": "1.0.0",
  "description": "Renders markdown passed in via REST and returns HTML",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node app.js"
  },
  "license": "ISC",
  "dependencies": {
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "markdown-it": "^10.0.0",
    "markdown-it-task-lists": "^2.1.1"
  }
}
